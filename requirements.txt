# 한국어 버전 LLM Needle In A Haystack 테스트를 위한 패키지

# ============================================
# 핵심 필수 패키지 (반드시 설치)
# ============================================
numpy>=1.24.0
python-dotenv>=1.0.0
jsonargparse>=4.27.0
tqdm>=4.66.0

# 결과 분석 및 시각화
matplotlib>=3.7.0
seaborn>=0.12.0

# OpenAI (기본 프로바이더 및 evaluator)
openai>=1.0.0
tiktoken>=0.5.0

# LangChain (OpenAI evaluator에서 사용)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.1.0

# ============================================
# 선택적 패키지 (필요시 설치)
# ============================================

# Anthropic 프로바이더 (Claude 모델 사용 시)
# pip install anthropic
anthropic>=0.18.0

# Cohere 프로바이더 (Command 모델 사용 시)
# pip install cohere langchain-cohere
cohere>=5.0.0
langchain-cohere>=0.1.0

# Google Gemini 프로바이더/Evaluator (Gemini 모델 사용 시)
# pip install google-genai
google-genai>=0.3.0

# PDF 처리 (PDF에서 텍스트 추출 시)
# pip install PyMuPDF Pillow
PyMuPDF>=1.23.0
Pillow>=10.0.0

# OCR (이미지에서 텍스트 추출 시 - DeepSeek-OCR 사용)
# 주의: vllm은 GPU 환경에서만 동작하며 설치가 복잡할 수 있습니다
# pip install vllm
# vllm>=0.3.0
